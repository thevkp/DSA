#include <iostream>
#include <vector>
using namespace std;

bool subarraySumZero(const vector<int>& arr){
    int n = 0;
    int sum = 0;
    for(int i = 1; i < n; i++){

        if(arr[i + 1] == complement)
            return true;
        else if(arr[i] )
    }
}

int main(){
    system("cls");

    vector<int> vec;

    int input;
    cin >> input;
    
    while(input != -1){
        vec.push_back(input);
        cin >> input;
    }

    cout << subarraySumZero(vec) << endl;



    return 0;
}

// 4 2 -3 1 6 -